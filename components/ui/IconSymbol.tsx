import React from 'react';
import { View } from 'react-native';

export function IconSymbol({ name, size, color, style }: any) {
  // lightweight stub used in project components for compile-time only
  return <View style={style} />;
}
